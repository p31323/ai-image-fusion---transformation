{
  "name": "ai-image-fusion-app-functions",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --jsx=automatic && cp index.html dist/"
  },
  "dependencies": {
    "@google/genai": "^1.16.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.6.0",
    "@types/node": "^20.11.20",
    "typescript": "^5.3.3",
    "esbuild": "^0.23.0"
  }
}
